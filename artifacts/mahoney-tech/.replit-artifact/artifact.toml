kind = "web"
previewPath = "/"
title = "Mahoney Tech Solutions"
version = "1.0.0"
id = "artifacts/mahoney-tech"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/mahoney-tech run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mahoney-tech", "run", "build" ]
publicDir = "artifacts/mahoney-tech/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
