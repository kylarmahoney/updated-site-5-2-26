[phases.install]
cmds = ['pnpm install --no-frozen-lockfile']

[phases.build]
cmds = [
  'PORT=3000 BASE_PATH=/ pnpm --filter @workspace/mahoney-tech run build',
  'pnpm --filter @workspace/api-server run build'
]

[start]
cmd = 'node artifacts/api-server/dist/index.mjs'
